export const appWideLog = true

export const serverConfigs = {
  baseUrl: 'https://swapi.py4e.com/api/',
  myRequestTimeout: 4000, //this only works FOR waiting for response WHEN CONNECTED,
  defaultTotalAxiosTries: 3,
  requireAccessToken: false,
}
